web:
  build: .
  volumes:
    - ./nginx:/etc/nginx
  ports:
    - "8082:80"
  environment:
    - AMP_HOST=https://xxx.com
  command: >
    /bin/sh -c
    "envsubst '
    $${AMP_HOST}
    '< /etc/nginx/nginx.conf.template
    > /etc/nginx/nginx.conf
    && nginx -g 'daemon off;'"
